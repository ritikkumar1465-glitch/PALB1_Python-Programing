def mergeArrays(self, a, b):
        n,m=len(a),len(b)
        i=n-1
        j=0
        while i>=0 and j<m:
            if a[i]>b[j]:
                a[i],b[j]=b[j],a[i]
                j+=1
            i-=1
        a.sort()
        b.sort()
